{
  "name": "eway-luxury-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix server install --legacy-peer-deps && npm --prefix client install --legacy-peer-deps && npm --prefix client run build"
  }
}
